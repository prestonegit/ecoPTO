---
const { news } = Astro.props;
---
<div class="border p-4 rounded-lg">
  <h3 class="text-xl font-bold">{news.data.title}</h3>
  <p>by {news.data.author} on {news.data.pubDate.toDateString()}</p>
  <a href={`/news/${news.slug}/`}>Read More</a>
</div>
