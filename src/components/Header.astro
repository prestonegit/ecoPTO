---
import globalSettings from '../content/settings/global.json';
---
<header class="bg-bg-primary backdrop-blur-md sticky top-0 z-50 border-b border-bg-secondary">
    <nav class="container mx-auto px-6 py-3 flex justify-between items-center">
    <div>
      <a href="/" class="text-2xl font-bold text-primary">
        <img src="/logo-icon.svg" alt="ecoPTO Logo" class="h-10 w-auto inline-block mr-2">eco<span class="text-secondary">PTO</span>
      </a>
    </div>
    
    <div>
      <button id="join-us-header-btn" class="bg-primary text-bg-primary px-5 py-2 rounded-full hover:bg-secondary transition-colors border-2 border-accent">
        Join Us
      </button>
    </div>
  </nav>
</header>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const joinUsBtn = document.getElementById('join-us-header-btn');
    const signupModal = document.getElementById('signup-modal');

    if (joinUsBtn && signupModal) {
      joinUsBtn.addEventListener('click', () => {
        signupModal.classList.add('modal-open');
      });
    }
  });
</script>