---
import Layout from '../layouts/Layout.astro';
import { getCollection } from "astro:content";
import TimelineItem from "../components/TimelineItem.astro";



const allPages = await getCollection("pages");
const impactPage = allPages.find(page => page.slug === "impact");

if (!impactPage) {
  return Astro.redirect("/404"); // Or render a fallback message
}

const { data, Content } = impactPage;
const { title, subtitle, achievements_title, achievements, image1, image2, image3, image4, project_highlight_pdf } = data;
---
<Layout title={title}>
  <section class="py-20 bg-primary">
    <div class="container mx-auto px-6 text-center">
      <h1 class="text-4xl font-bold text-secondary">{title}</h1>
      <p class="mt-4 text-lg text-bg-primary max-w-3xl mx-auto">{subtitle}</p>
      
      {project_highlight_pdf && (
        <div class="mt-8">
          <a 
            href={project_highlight_pdf} 
            target="_blank" 
            rel="noopener noreferrer"
            class="inline-block bg-accent hover:bg-opacity-90 text-white font-bold py-3 px-8 rounded-lg transition duration-300 transform hover:scale-105 shadow-md"
          >
            View Project Highlight
          </a>
        </div>
      )}
    </div>
  </section>

  <section class="py-20 relative">
    <div class="container mx-auto px-6">
      
      <!-- Timeline Container -->
      <div class="relative wrap overflow-hidden p-10 h-full">
        <div class="border-l-4 border-primary absolute h-full left-1/2 transform -translate-x-1/2"></div>
        
        {achievements?.map((achievement, index) => (
          <TimelineItem achievement={achievement} index={index} />
        ))}
      </div>
    </div>
  </section>

  

  {Content && <Content />}
</Layout>